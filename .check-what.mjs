import { chromium } from "playwright";
const OUT = "/private/tmp/claude-501/-Users-kohei-workspace-rusutsu/aa69cffc-d60f-48c2-a2e3-0d310a4afe1e/scratchpad";
const browser = await chromium.launch();
const page = await browser.newPage({ viewport: { width: 1440, height: 900 } });
await page.goto("http://localhost:3000/rusutsu/admin/slope", { waitUntil: "networkidle", timeout: 60000 });
await page.waitForTimeout(2000);
console.log("url:", page.url());
console.log("title:", await page.title());
console.log("body:", (await page.locator("body").innerText()).slice(0, 600));
await page.screenshot({ path: `${OUT}/admin-landing.png` });
await browser.close();
