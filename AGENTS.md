# Project Notes

- The app is served under the Next.js base path `/rusutsu`.
- When opening the local app in a browser or Playwright, use `http://localhost:3000/rusutsu`.
- Do not use `http://localhost:3000` for app verification because `/` returns a 404.
