services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rusutsu-app
    ports:
      - "127.0.0.1:${PORT:-3000}:3000"
    environment:
      - NODE_ENV=production
    env_file:
      - .env
    restart: unless-stopped
